import React, { useState, useEffect } from 'react'
import DefaultLayout from '../../layout/Layout'

const ProductTag = props => {
    useEffect( () => {})

    return (
        <DefaultLayout>
        Product Tag
        </DefaultLayout>
    )
}

productTag.getInitialProps = async (ctx) => {
    return {
        
    }
}

export default ProductTag;