const wishlistInitialState = {
    items: [],
};

export const wishlistReducer = (state = wishlistInitialState, action) => {
    switch(action.type) {
        default:
            return state;
    } 
}