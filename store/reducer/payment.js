const paymentInitialState = {
    gateways: []
}

export const paymentReducer = ( state = paymentInitialState, action ) => {
    switch(action.type) {
        default:
            return state;
    }
}