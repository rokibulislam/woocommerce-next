const countryInitialState = {
    items: []
}

export const countryReducer = ( state = countryInitialState, action ) => {
    switch(action.type) {
        default:
            return state;  
    }
}