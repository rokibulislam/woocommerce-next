const shippingInitialState = {
    data: []
}

export const shippingReducer = ( state = shippingInitialState, action ) => {
    switch(action.type) {
        default:
            return state;
    }
}
