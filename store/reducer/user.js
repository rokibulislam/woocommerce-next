const currentUserInitialState = {
    data: false
}

export const userReducer = ( state = currentUserInitialState, action ) => {
    switch(action.type) {
        default:
            return state;
    }
}